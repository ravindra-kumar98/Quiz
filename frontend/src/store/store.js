import { configureStore } from '@reduxjs/toolkit';  // import configureStore from redux toolkit


export default configureStore({
    reducer : {},
});  // export default configureStore with no arguments